---
title: Lea Who Codes
description: "Lea's Blog on Web Development with Accessibility in Mind"
layout: base.njk
author: Lea Rosema
eleventyExcludeFromCollections: true
---

<div class="intro">
  <article class="flow wrapper">
    <header class="intro__header">
      <p>Hello World! I am the</p>
      <h1>{{ title }}</h1>
    </header>
    <p class="intro__text">I'm a frontend developer, accessibility lover and digital artist based in Hamburg.</p>
  </article>
</div>
<svg class="intro-bottom" viewBox="0 0 1200 150" aria-hidden="true" preserveAspectRatio="none">
  {{ prng.init(342) }}
  <path class="s" d="{{ prng.curve(7, 150, 50, 25) }} V0H0Z"/>
  <path class="s"  d="{{ prng.curve(7, 150, 65, 25) }} V0H0Z" fill-opacity="0.6"/>
  <path class="s"  d="{{ prng.curve(7, 150, 80, 25) }} V0H0Z" fill-opacity="0.2"/>

  <path class="md" d="{{ prng.curve(11, 100, 50, 25) }} V0H0Z"/>
  <path class="md"  d="{{ prng.curve(11, 100, 65, 25) }} V0H0Z" fill-opacity="0.6"/>
  <path class="md"  d="{{ prng.curve(11, 100, 80, 25) }} V0H0Z" fill-opacity="0.2"/>
</svg>

<div class="article-list">
  <article class="flow wrapper">
    <h2>Articles</h2>
    <ul role="list" class="auto-grid">
    {% for post in collections.posts | reverse %}
      <li class="article-card | wrapper flow prose">
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <p>{{ post.data.description }}</p>
      </li>
    {% endfor %}
    </ul>
  </div>
</div>
